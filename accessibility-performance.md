# ⚡️ Performance & Accessibility Standards

Цей документ встановлює стандарти продуктивності та доступності для фронтенд-проєктів, побудованих на TypeScript/JavaScript, Vue, Vuetify, CSS та Sass. Його мета — забезпечити швидке завантаження, плавну взаємодію та доступність для всіх користувачів.

---

## ⚡️ Performance Standards

### 📦 Загальні принципи

- [ ] Мінімізувати розмір бандлу (`< 250KB` для початкового завантаження)
- [ ] Використовувати **code splitting** (`defineAsyncComponent`, `dynamic import`)
- [ ] Уникати **неоптимізованих сторонніх бібліотек**
- [ ] Використовувати **tree-shaking** для видалення невикористаного коду

### 🧱 Компоненти та шаблони

- [ ] Уникати глибоких вкладень компонентів (> 5 рівнів)
- [ ] Використовувати `v-if` замість `v-show` для умовного рендерингу, якщо елемент не потрібен
- [ ] Уникати повторного рендерингу через зайві реактивні залежності

### 🎨 Стилі та ресурси

- [ ] Використовувати **Sass-перемінні** для кольорів, шрифтів, розмірів
- [ ] Уникати великих зображень без оптимізації (WebP, SVG, lazy loading)
- [ ] Використовувати `font-display: swap` для шрифтів
- [ ] Мінімізувати кількість HTTP-запитів

### 🧪 Тестування продуктивності

- [ ] Використовувати [Lighthouse](https://developer.chrome.com/docs/lighthouse/overview/) для аудиту
- [ ] Цільовий **Performance Score**: ≥ 90
- [ ] Перевіряти **Time to Interactive (TTI)**, **Largest Contentful Paint (LCP)**, **First Input Delay (FID)**

---

## ♿ Accessibility Standards

### 🌐 Загальні принципи

- [ ] Всі користувачі повинні мати доступ до контенту, незалежно від пристрою чи можливостей
- [ ] Всі компоненти мають бути **доступними з клавіатури**
- [ ] Всі інтерактивні елементи мають **ARIA-атрибути**, якщо нативні не підходять

### 🧩 Компоненти Vuetify

- [ ] Використовувати **Vuetify компоненти**, які вже мають вбудовану підтримку доступності
- [ ] Перевіряти кастомні компоненти на відповідність WCAG 2.1

### 🧠 Семантика та структура

- [ ] Використовувати семантичні HTML-елементи (`<main>`, `<nav>`, `<section>`, `<button>`)
- [ ] Уникати `<div>` для інтерактивних елементів без ролей
- [ ] Всі зображення мають `alt`-текст

### 🎛️ Фокус та навігація

- [ ] Забезпечити **видимий фокус** для всіх елементів
- [ ] Використовувати `tabindex` для контролю порядку навігації
- [ ] Уникати фокус-пасток (наприклад, модальні вікна без escape)

### 🔊 Читачі екрану

- [ ] Всі повідомлення мають бути доступні через `aria-live`
- [ ] Всі форми мають мітки (`<label>`) або `aria-label`
- [ ] Перевіряти компоненти з [NVDA](https://www.nvaccess.org/) або [VoiceOver](https://support.apple.com/voiceover)

### 🧪 Тестування доступності

- [ ] Використовувати [axe](https://www.deque.com/axe/) або [Pa11y](https://pa11y.org/) для автоматичної перевірки
- [ ] Цільовий **Accessibility Score (Lighthouse)**: ≥ 95
- [ ] Перевіряти вручну: клавіатурна навігація, читачі екрану, контраст

---

## 📌 Відповідальність

- **Кожен розробник** відповідає за продуктивність і доступність свого коду
- **Code Review** включає перевірку на відповідність цим стандартам
- **CI/CD** має включати автоматичні перевірки Lighthouse та axe
